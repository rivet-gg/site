import { ResourceGroup, Resource } from '@/components/Resources';
import { faServer, faCircleNodes } from '@fortawesome/sharp-solid-svg-icons';

# Low-Level API

Rivet provides access to the APIs used by our engine plugins and Rivet modules should you need to use them.
These docs explain the API usage of and concepts within our underlying API.

<ResourceGroup>
  <Resource title='Dynamic Servers' icon={faServer} href='/docs/core/dynamic-servers/overview'>
    Auto-scaling game lobbies boot up to meet demand.
  </Resource>
  <Resource title='Dynamic Servers API' icon={faCircleNodes} href='/docs/core/dynamic-servers/api/create'>
    Auto-scaling game lobbies boot up to meet demand.
  </Resource>
</ResourceGroup>
