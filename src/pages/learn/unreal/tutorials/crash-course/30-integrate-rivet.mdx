# Step X: Integrate with Rivet

## Add the Rivet module

- TODO: Copy module
- TODO: Update uproject
- TODO: Update Build.cs

## Update `AMyProjectGameMode` class

We will update the game mode code to connect player connect & disconnect events to Rivet.

**Source/MyProject/MyProjectGameMode.h**

1. Open the file `Source/MyProject/MyProjectGameMode.h` (replace `MyProject` with your project name)
2. TODO

**Source/MyProject/MyProjectGameMode.cpp**

1. Open the file `Source/MyProject/MyProjectGameMode.cpp` (replace `MyProject` with your project name)
2. TODO

## Add development token to config

We will configure the token to connect to Rivet to test the game.

1. Create a development token by running:

   ```bash
   rivet dev create-dev-token
   ```

2. Add the following to the end of _Config/DefaultGame.ini_. Replace `MyProject` with your project name and replace `YOUR_TOKEN_HERE` with the token created in the last step.

   ```ini Config/DefaultGame.ini
   [/Script/MyProject.RivetClient]
   DefaultToken=YOUR_TOKEN_HERE
   ```

## Test the game

1. Open the _Entry_ world.
2. Press _Play_.
3. Click the _Connect_ button you created to call the Rivet API and connect to your local server.

If you run into issues, check the log for warnings and errors.

This is using the Rivet development token to mock production APIs. This lets you test with production APIs while still using your local machine.
