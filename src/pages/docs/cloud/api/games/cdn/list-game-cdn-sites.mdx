
import { CodeGroup, Code } from '@/components/Code';

# games.cdn.listGameCdnSites

## Description

Lists CDN sites for a game.


## Code Examples

<CodeGroup title='Request' tag='GET' label='https://api.rivet.gg/cloud/games/{game_id}/cdn/sites'>

```bash {{ "title": "cURL" }}
curl -X GET 'https://api.rivet.gg/cloud/games/{game_id}/cdn/sites'
```

```ts
// Create Rivet client
import { RivetClient } from '@rivet-gg/api';
const RIVET = new RivetClient({ token: addYourTokenHere });

// Make request
await RIVET.cloud.games.cdn.listGameCdnSites({
  // Add your request body here
});
```

</CodeGroup>


## Request Parameters


### `game_id`

_Path parameter, required_





## Response Body

### `sites`

`array<object>` (required)

A list of CDN site summaries.

### `sites[*].complete`

`boolean` (required)

Whether or not this site has completely been uploaded.

### `sites[*].content_length`

`integer` (required)

Unsigned 64 bit integer.

### `sites[*].create_ts`

`string` (required)

RFC3339 timestamp.

### `sites[*].display_name`

`string` (required)

Represent a resource's readable display name.

### `sites[*].site_id`

`string` (required)

### `sites[*].upload_id`

`string` (required)



