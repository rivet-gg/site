
import { CodeGroup, Code } from '@/components/Code';
import { DocsJsonSchemaViewer } from '@/components/DocsJsonSchemaViewer';

# games.versions.reserveVersionName

## Description

Reserves a display name for the next version. Used to generate a monotomically increasing build number without causing a race condition with multiple versions getting created at the same time.


## Code Examples

<CodeGroup title='Request' tag='POST' label='https://api.rivet.gg/cloud/games/{game_id}/versions/reserve-name'>

```bash {{ title: 'cURL' }}
curl -X POST 'https://api.rivet.gg/cloud/games/{game_id}/versions/reserve-name'
```

```ts
// Create Rivet client
import { RivetClient } from '@rivet-gg/api';
const RIVET = new RivetClient({ token: addYourTokenHere });

// Make request
await RIVET.cloud.games.versions.reserveVersionName({
  // Add your request body here
});
```

</CodeGroup>


## Request Parameters


### game_id

_Path parameter, required_





## Response Body

<DocsJsonSchemaViewer schema={{"type":"object","properties":{"version_display_name":{"type":"string"}},"required":["version_display_name"]}} />

