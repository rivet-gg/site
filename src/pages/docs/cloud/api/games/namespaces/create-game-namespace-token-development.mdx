
import { CodeGroup, Code } from '@/components/Code';

# games.namespaces.createGameNamespaceTokenDevelopment

## Description

Creates a development token for the given namespace.


## Code Examples

<CodeGroup title='Request' tag='POST' label='https://api.rivet.gg/cloud/games/{game_id}/namespaces/{namespace_id}/tokens/development'>

```bash {{ "title": "cURL" }}
# Write the request body to body.json before running
curl -X POST -d '@body.json' 'https://api.rivet.gg/cloud/games/{game_id}/namespaces/{namespace_id}/tokens/development'
```

```ts
// Create Rivet client
import { RivetClient } from '@rivet-gg/api';
const RIVET = new RivetClient({ token: addYourTokenHere });

// Make request
await RIVET.cloud.games.namespaces.createGameNamespaceTokenDevelopment({
  // Add your request body here
});
```

</CodeGroup>


## Request Parameters


### `game_id`

_Path parameter, required_





### `namespace_id`

_Path parameter, required_





## Request Body

### `hostname`

`string` (required)

The hostname used for the token.

### `ports`

`map<string, object>`

### `ports.<port>.port`

`integer`

### `ports.<port>.port_range`

`object`

Range of ports that can be connected to.
If configured, `network_mode` must equal `host`.
Port ranges may overlap between containers, it is the responsibility of the developer to ensure ports are available before using.
Read more about host networking [here](https://rivet.gg/docs/dynamic-servers/concepts/host-bridge-networking).
Only available on Rivet Open Source & Enterprise.

### `ports.<port>.port_range.max`

`integer` (required)

Unsigned 32 bit integer.

### `ports.<port>.port_range.min`

`integer` (required)

Unsigned 32 bit integer.

### `ports.<port>.protocol`

`string` (required)

Type of network traffic to allow access to this port.
Configuring `https` or `tcp_tls` will provide TLS termination for you via Game Guard.
`https` and `tcp_tls` must have `proxy_kind` set to `game_guard`.




## Response Body

### `token`

`string` (required)

A JSON Web Token.
Slightly modified to include a description prefix and use Protobufs of
JSON.



