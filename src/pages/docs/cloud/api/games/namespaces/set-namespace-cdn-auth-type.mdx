
import { CodeGroup, Code } from '@/components/Code';
import { DocsJsonSchemaViewer } from '@/components/DocsJsonSchemaViewer';

# games.namespaces.setNamespaceCdnAuthType

## Description

Updates the CDN authentication type of the given game namesapce.


## Code Example

<CodeGroup title='Request' tag='PUT' label='https://cloud.api.rivet.gg/v1/games/{game_id}/namespaces/{namespace_id}/cdn-auth'>

```bash {{ title: 'cURL' }}
# Write the request body to body.json before running
curl -X PUT -d '@body.json' 'https://cloud.api.rivet.gg/v1/games/{game_id}/namespaces/{namespace_id}/cdn-auth'
```

```ts
// Create Rivet client
import { RivetClient } from '@rivet-gg/api';
const RIVET = new RivetClient({ token: addYourTokenHere });

// Make request
await RIVET.cloud.games.namespaces.setNamespaceCdnAuthType({
  // Add your request body here
});
```

</CodeGroup>


## Request Body

<DocsJsonSchemaViewer
  schema={{"type":"object","properties":{"auth_type":{"type":"string","enum":["none","basic"],"description":"A value denoting what type of authentication to use for a game namespace's CDN."}},"required":["auth_type"]}}
  expanded={true}
  hideTopBar={false}
  emptyText="No schema defined"
  defaultExpandedDepth={0}
/>


## Response Body

_Empty response body._
