
import { CodeGroup, Code } from '@/components/Code';

# groups.validate

## Description

Validates information used to create a new group.


## Code Examples

<CodeGroup title='Request' tag='POST' label='https://api.rivet.gg/cloud/groups/validate'>

```bash {{ "title": "cURL" }}
# Write the request body to body.json before running
curl -X POST -d '@body.json' 'https://api.rivet.gg/cloud/groups/validate'
```

```ts
// Create Rivet client
import { RivetClient } from '@rivet-gg/api';
const RIVET = new RivetClient({ token: addYourTokenHere });

// Make request
await RIVET.cloud.groups.validate({
  // Add your request body here
});
```

</CodeGroup>


## Request Parameters


## Request Body

### `display_name`

`string` (required)

Represent a resource's readable display name.




## Response Body

### `errors`

`array<object>` (required)

A list of validation errors.

### `errors[*].path`

`array<string>` (required)

A list of strings denoting the origin of a validation error.



