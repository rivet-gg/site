
import { CodeGroup, Code } from '@/components/Code';

# ⭐️ get

<ExperimentalFeature />

## Description

Returns a specific key-value entry by key.


## Code Examples

<CodeGroup title='Request' tag='GET' label='https://api.rivet.gg/kv/entries'>

```bash {{ "title": "cURL" }}
curl -X GET 'https://api.rivet.gg/kv/entries'
```

```ts
// Create Rivet client
import { RivetClient } from '@rivet-gg/api';
const RIVET = new RivetClient({ token: '[YOUR TOKEN HERE]' });

// Make request
await RIVET.kv.get({
  // Add your request body here
});
```

</CodeGroup>


## Request Parameters


### `key`

_Query parameter, required_





### `watch_index`

_Query parameter, optional_


A query parameter denoting the requests watch index.


### `namespace_id`

_Query parameter, optional_





## Response Body

### `deleted`

`boolean`

Whether or not the entry has been deleted. Only set when watching this endpoint.

### `value`

`object` (required)

A JSON object stored in the KV database.
A `null` value indicates the entry is deleted.
Maximum length of 262,144 bytes when encoded.

### `watch`

`object` (required)

Provided by watchable endpoints used in blocking loops.

### `watch.index`

`string` (required)

Index indicating the version of the data responded.
Pass this to `WatchQuery` to block and wait for the next response.



