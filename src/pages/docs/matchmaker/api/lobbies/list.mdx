import { CodeGroup, Code } from '@/components/Code';

# lobbies.list

## Description

Lists all open lobbies.


## Code Example

<CodeGroup title='Request' tag='GET' label='https://matchmaker.api.rivet.gg/v1/lobbies/list'>

```bash {{ title: 'cURL' }}
curl -X GET 'https://matchmaker.api.rivet.gg/v1/lobbies/list'
```

```ts
// Create Rivet client
import { RivetClient } from '@rivet-gg/api';
const RIVET = new RivetClient({ token: addYourTokenHere });

// Make request
await RIVET.matchmaker.lobbies.list({
  // Add your request body here
});
```

</CodeGroup>


## Response Body

_Represented in [JSON Schema](https://json-schema.org/)._

<CodeGroup title='Response'>

```yaml
type: object
properties:
  game_modes:
    type: array
    items:
      type: object
      description: A game mode that the player can join.
      properties:
        game_mode_id:
          type: string
          description: A human readable short identifier used to references resources.
            Different than a `uuid` because this is intended to be human
            readable. Different than `DisplayName` because this should not
            include special characters and be short.
      required:
        - game_mode_id
  regions:
    type: array
    items:
      type: object
      description: A region that the player can connect to.
      properties:
        region_id:
          type: string
          description: A human readable short identifier used to references resources.
            Different than a `uuid` because this is intended to be human
            readable. Different than `DisplayName` because this should not
            include special characters and be short.
        provider_display_name:
          type: string
        region_display_name:
          type: string
        datacenter_coord:
          type: object
          description: Geographical coordinates for a location on Planet Earth.
          properties:
            latitude:
              type: number
              format: double
            longitude:
              type: number
              format: double
          required:
            - latitude
            - longitude
        datacenter_distance_from_client:
          type: object
          description: Distance available in multiple units.
          properties:
            kilometers:
              type: number
              format: double
            miles:
              type: number
              format: double
          required:
            - kilometers
            - miles
      required:
        - region_id
        - provider_display_name
        - region_display_name
        - datacenter_coord
        - datacenter_distance_from_client
  lobbies:
    type: array
    items:
      type: object
      description: A public lobby in the lobby list.
      properties:
        region_id:
          type: string
        game_mode_id:
          type: string
        lobby_id:
          type: string
          format: uuid
        max_players_normal:
          type: integer
        max_players_direct:
          type: integer
        max_players_party:
          type: integer
        total_player_count:
          type: integer
      required:
        - region_id
        - game_mode_id
        - lobby_id
        - max_players_normal
        - max_players_direct
        - max_players_party
        - total_player_count
required:
  - game_modes
  - regions
  - lobbies

```

```json
{
  "type": "object",
  "properties": {
    "game_modes": {
      "type": "array",
      "items": {
        "type": "object",
        "description": "A game mode that the player can join.",
        "properties": {
          "game_mode_id": {
            "type": "string",
            "description": "A human readable short identifier used to references resources. Different than a `uuid` because this is intended to be human readable. Different than `DisplayName` because this should not include special characters and be short."
          }
        },
        "required": [
          "game_mode_id"
        ]
      }
    },
    "regions": {
      "type": "array",
      "items": {
        "type": "object",
        "description": "A region that the player can connect to.",
        "properties": {
          "region_id": {
            "type": "string",
            "description": "A human readable short identifier used to references resources. Different than a `uuid` because this is intended to be human readable. Different than `DisplayName` because this should not include special characters and be short."
          },
          "provider_display_name": {
            "type": "string"
          },
          "region_display_name": {
            "type": "string"
          },
          "datacenter_coord": {
            "type": "object",
            "description": "Geographical coordinates for a location on Planet Earth.",
            "properties": {
              "latitude": {
                "type": "number",
                "format": "double"
              },
              "longitude": {
                "type": "number",
                "format": "double"
              }
            },
            "required": [
              "latitude",
              "longitude"
            ]
          },
          "datacenter_distance_from_client": {
            "type": "object",
            "description": "Distance available in multiple units.",
            "properties": {
              "kilometers": {
                "type": "number",
                "format": "double"
              },
              "miles": {
                "type": "number",
                "format": "double"
              }
            },
            "required": [
              "kilometers",
              "miles"
            ]
          }
        },
        "required": [
          "region_id",
          "provider_display_name",
          "region_display_name",
          "datacenter_coord",
          "datacenter_distance_from_client"
        ]
      }
    },
    "lobbies": {
      "type": "array",
      "items": {
        "type": "object",
        "description": "A public lobby in the lobby list.",
        "properties": {
          "region_id": {
            "type": "string"
          },
          "game_mode_id": {
            "type": "string"
          },
          "lobby_id": {
            "type": "string",
            "format": "uuid"
          },
          "max_players_normal": {
            "type": "integer"
          },
          "max_players_direct": {
            "type": "integer"
          },
          "max_players_party": {
            "type": "integer"
          },
          "total_player_count": {
            "type": "integer"
          }
        },
        "required": [
          "region_id",
          "game_mode_id",
          "lobby_id",
          "max_players_normal",
          "max_players_direct",
          "max_players_party",
          "total_player_count"
        ]
      }
    }
  },
  "required": [
    "game_modes",
    "regions",
    "lobbies"
  ]
}
```

</CodeGroup>

