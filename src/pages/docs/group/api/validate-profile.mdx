
import { CodeGroup, Code } from '@/components/Code';

# validateProfile

## Description

Validate contents of group profile. Use to provide immediate feedback on profile changes before committing them.


## Code Examples

<CodeGroup title='Request' tag='POST' label='https://api.rivet.gg/group/groups/profile/validate'>

```bash {{ title: 'cURL' }}
# Write the request body to body.json before running
curl -X POST -d '@body.json' 'https://api.rivet.gg/group/groups/profile/validate'
```

```ts
// Create Rivet client
import { RivetClient } from '@rivet-gg/api';
const RIVET = new RivetClient({ token: addYourTokenHere });

// Make request
await RIVET.group.validateProfile({
  // Add your request body here
});
```

</CodeGroup>


## Request Parameters


## Request Body

### `bio`

`string`

Detailed information about a profile.

### `display_name`

`string`

Represent a resource's readable display name.

### `publicity`

`string`

The current publicity value for the given group.




## Response Body

### `errors`

`array<object>` (required)

A list of validation errors.

### `errors[*].path`

`array<string>` (required)

A list of strings denoting the origin of a validation error.



