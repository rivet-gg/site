
import { CodeGroup, Code } from '@/components/Code';
import { DocsJsonSchemaViewer } from '@/components/DocsJsonSchemaViewer';

# invites.getInvite

## Description

Inspects a group invite returning information about the team that created it.


## Code Examples

<CodeGroup title='Request' tag='GET' label='https://group.api.rivet.gg/v1/invites/{group_invite_code}'>

```bash {{ title: 'cURL' }}
curl -X GET 'https://group.api.rivet.gg/v1/invites/{group_invite_code}'
```

```ts
// Create Rivet client
import { RivetClient } from '@rivet-gg/api';
const RIVET = new RivetClient({ token: addYourTokenHere });

// Make request
await RIVET.group.invites.getInvite({
  // Add your request body here
});
```

</CodeGroup>


## Request Parameters


### group_invite_code

_Path parameter, required_


Provided by `rivet.api.group#CreateInviteResponse$code`.


## Response Body

<DocsJsonSchemaViewer schema={{"type":"object","properties":{"group":{"type":"object","description":"A group handle.","properties":{"group_id":{"type":"string","format":"uuid"},"display_name":{"type":"string"},"avatar_url":{"type":"string","description":"The URL of this group's avatar image"},"external":{"type":"object","description":"External links for this group.","properties":{"profile":{"type":"string","description":"A link to this group's profile page."},"chat":{"type":"string","description":"A link to this group's chat page."}},"required":["profile","chat"]},"is_developer":{"type":"boolean","description":"Whether or not this group is a developer group."}},"required":["group_id","display_name","external"]}},"required":["group"]}} />

