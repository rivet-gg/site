
import { CodeGroup, Code } from '@/components/Code';
import { DocsJsonSchemaViewer } from '@/components/DocsJsonSchemaViewer';

# ⭐️ get

## Description

Returns a specific key-value entry by key.


## Code Examples

<CodeGroup title='Request' tag='GET' label='https://api.rivet.gg/kv/entries'>

```bash {{ title: 'cURL' }}
curl -X GET 'https://api.rivet.gg/kv/entries'
```

```ts
// Create Rivet client
import { RivetClient } from '@rivet-gg/api';
const RIVET = new RivetClient({ token: addYourTokenHere });

// Make request
await RIVET.kv.get({
  // Add your request body here
});
```

</CodeGroup>


## Request Parameters


### key

_Query parameter, required_





### watch_index

_Query parameter, optional_


A query parameter denoting the requests watch index.


### namespace_id

_Query parameter, optional_





## Response Body

<DocsJsonSchemaViewer schema={{"type":"object","properties":{"value":{"description":"A JSON object stored in the KV database.\nA `null` value indicates the entry is deleted.\nMaximum length of 262,144 bytes when encoded."},"deleted":{"type":"boolean","description":"Whether or not the entry has been deleted. Only set when watching this endpoint."},"watch":{"type":"object","description":"Provided by watchable endpoints used in blocking loops.","properties":{"index":{"type":"string","description":"Index indicating the version of the data responded.\nPass this to `WatchQuery` to block and wait for the next response.\n"}},"required":["index"]}},"required":["value","watch"]}} />

