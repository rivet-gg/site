import { CodeGroup, Code } from '@/components/Code';

# setThreadRead

## Description

Updates the current identity's last read timestamp in the given thread.


## Code Example

<CodeGroup title='Request' tag='POST' label='https://chat.api.rivet.gg/v1/threads/{thread_id}/read'>

```bash {{ title: 'cURL' }}
# Write the request body to body.json before running
curl -X POST -d '@body.json' 'https://chat.api.rivet.gg/v1/threads/{thread_id}/read'
```

```ts
// Create Rivet client
import { RivetClient } from '@rivet-gg/api';
const RIVET = new RivetClient({ token: addYourTokenHere });

// Make request
await RIVET.chat.setThreadRead({
  // Add your request body here
});
```

</CodeGroup>


## Request Body

_Represented in [JSON Schema](https://json-schema.org/)._

<CodeGroup title='Request'>

```yaml
type: object
properties:
  last_read_ts:
    type: string
    format: date-time
    description: |-
      Any messages newer than this timestamp will be marked as unread.
      This should be the current timestamp (in milliseconds).
required:
  - last_read_ts

```

```json
{
  "type": "object",
  "properties": {
    "last_read_ts": {
      "type": "string",
      "format": "date-time",
      "description": "Any messages newer than this timestamp will be marked as unread.\nThis should be the current timestamp (in milliseconds)."
    }
  },
  "required": [
    "last_read_ts"
  ]
}
```

</CodeGroup>


## Response Body

_Empty response body._
