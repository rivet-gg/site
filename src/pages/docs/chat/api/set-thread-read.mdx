
import { CodeGroup, Code } from '@/components/Code';
import { DocsJsonSchemaViewer } from '@/components/DocsJsonSchemaViewer';

# setThreadRead

## Description

Updates the current identity's last read timestamp in the given thread.


## Code Example

<CodeGroup title='Request' tag='POST' label='https://chat.api.rivet.gg/v1/threads/{thread_id}/read'>

```bash {{ title: 'cURL' }}
# Write the request body to body.json before running
curl -X POST -d '@body.json' 'https://chat.api.rivet.gg/v1/threads/{thread_id}/read'
```

```ts
// Create Rivet client
import { RivetClient } from '@rivet-gg/api';
const RIVET = new RivetClient({ token: addYourTokenHere });

// Make request
await RIVET.chat.setThreadRead({
  // Add your request body here
});
```

</CodeGroup>


## Request Body

<DocsJsonSchemaViewer
  schema={{"type":"object","properties":{"last_read_ts":{"type":"string","format":"date-time","description":"Any messages newer than this timestamp will be marked as unread.\nThis should be the current timestamp (in milliseconds)."}},"required":["last_read_ts"]}}
  expanded={true}
  hideTopBar={false}
  emptyText="No schema defined"
  defaultExpandedDepth={0}
/>


## Response Body

_Empty response body._
