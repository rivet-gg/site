
import { CodeGroup, Code } from '@/components/Code';
import { DocsJsonSchemaViewer } from '@/components/DocsJsonSchemaViewer';

# getThreadTopic

## Description

Fetches the topic of a thread.


## Code Example

<CodeGroup title='Request' tag='GET' label='https://chat.api.rivet.gg/v1/threads/{thread_id}/topic'>

```bash {{ title: 'cURL' }}
curl -X GET 'https://chat.api.rivet.gg/v1/threads/{thread_id}/topic'
```

```ts
// Create Rivet client
import { RivetClient } from '@rivet-gg/api';
const RIVET = new RivetClient({ token: addYourTokenHere });

// Make request
await RIVET.chat.getThreadTopic({
  // Add your request body here
});
```

</CodeGroup>


## Response Body

<DocsJsonSchemaViewer
  schema={{"type":"object","properties":{"topic":{"type":"object","description":"Represents a topic of the given chat thread without the associated handles\nfor the topic.\n","properties":{"group":{"type":"object","properties":{"group":{"type":"string","format":"uuid"}},"required":["group"]},"party":{"type":"object","properties":{"party":{"type":"string","format":"uuid"}},"required":["party"]},"direct":{"type":"object","properties":{"identity_a":{"type":"string","format":"uuid"},"identity_b":{"type":"string","format":"uuid"}},"required":["identity_a","identity_b"]}}}},"required":["topic"]}}
  expanded={true}
  hideTopBar={false}
  emptyText="No schema defined"
  defaultExpandedDepth={0}
/>

