
import { CodeGroup, Code } from '@/components/Code';
import { DocsJsonSchemaViewer } from '@/components/DocsJsonSchemaViewer';

# setTypingStatus

## Description

Updates the current identity's typing status in the given thread.


## Code Example

<CodeGroup title='Request' tag='PUT' label='https://chat.api.rivet.gg/v1/threads/{thread_id}/typing-status'>

```bash {{ title: 'cURL' }}
# Write the request body to body.json before running
curl -X PUT -d '@body.json' 'https://chat.api.rivet.gg/v1/threads/{thread_id}/typing-status'
```

```ts
// Create Rivet client
import { RivetClient } from '@rivet-gg/api';
const RIVET = new RivetClient({ token: addYourTokenHere });

// Make request
await RIVET.chat.setTypingStatus({
  // Add your request body here
});
```

</CodeGroup>


## Request Body

<DocsJsonSchemaViewer
  schema={{"type":"object","properties":{"status":{"type":"object","properties":{"idle":{"type":"object","properties":{},"description":"Not typing"},"typing":{"type":"object","properties":{},"description":"Typing"}}}},"required":["status"]}}
  expanded={true}
  hideTopBar={false}
  emptyText="No schema defined"
  defaultExpandedDepth={0}
/>


## Response Body

_Empty response body._
