
import { CodeGroup, Code } from '@/components/Code';
import { DocsJsonSchemaViewer } from '@/components/DocsJsonSchemaViewer';

# setGameActivity

## Description

Sets the current identity's game activity. This activity will automatically be removed when the identity goes offline.


## Code Example

<CodeGroup title='Request' tag='POST' label='https://identity.api.rivet.gg/v1/identities/self/activity'>

```bash {{ title: 'cURL' }}
# Write the request body to body.json before running
curl -X POST -d '@body.json' 'https://identity.api.rivet.gg/v1/identities/self/activity'
```

```ts
// Create Rivet client
import { RivetClient } from '@rivet-gg/api';
const RIVET = new RivetClient({ token: addYourTokenHere });

// Make request
await RIVET.identity.setGameActivity({
  // Add your request body here
});
```

</CodeGroup>


## Request Body

<DocsJsonSchemaViewer
  schema={{"type":"object","properties":{"game_activity":{"type":"object","description":"Information about the identity's current game. This is information that all other identities can see about what the current identity is doing.","properties":{"message":{"type":"string","description":"A short message about the current game activity."},"public_metadata":{"description":"JSON data seen by anyone."},"mutual_metadata":{"description":"JSON data seen only by the given identity and their mutual followers."}}}},"required":["game_activity"]}}
  expanded={true}
  hideTopBar={false}
  emptyText="No schema defined"
  defaultExpandedDepth={0}
/>


## Response Body

_Empty response body._
