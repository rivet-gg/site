import { CodeGroup, Code } from '@/components/Code';

# updateProfile

## Code Example

<CodeGroup title='Request' tag='POST' label='https://identity.api.rivet.gg/v1/identities/self/profile'>

```bash {{ title: 'cURL' }}
# Write the request body to body.json before running
curl -X POST -d '@body.json' 'https://identity.api.rivet.gg/v1/identities/self/profile'
```

```ts
// Create Rivet client
import { RivetClient } from '@rivet-gg/api';
const RIVET = new RivetClient({ token: addYourTokenHere });

// Make request
await RIVET.identity.updateProfile({
  // Add your request body here
});
```

</CodeGroup>


## Request Body

_Represented in [JSON Schema](https://json-schema.org/)._

<CodeGroup title='Request'>

```yaml
type: object
properties:
  display_name:
    type: string
  account_number:
    type: integer
  bio:
    type: string
    description: Follows regex ^(?:[^\n\r]+\n?|\n){1,5}$

```

```json
{
  "type": "object",
  "properties": {
    "display_name": {
      "type": "string"
    },
    "account_number": {
      "type": "integer"
    },
    "bio": {
      "type": "string",
      "description": "Follows regex ^(?:[^\\n\\r]+\\n?|\\n){1,5}$"
    }
  }
}
```

</CodeGroup>


## Response Body

_Empty response body._
