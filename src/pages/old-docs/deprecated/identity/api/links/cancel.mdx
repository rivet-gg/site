import { CodeGroup, Code } from '@/components/Code';

# links.cancel

## Description

Cancels a game link. It can no longer be used to link after cancellation.

## Code Examples

<CodeGroup title='Request' tag='POST' label='https://api.rivet.gg/identity/game-links/cancel'>

```bash {{ "title": "cURL" }}
# Write the request body to body.json before running
curl -X POST -d '@body.json' 'https://api.rivet.gg/identity/game-links/cancel'
```

```ts
// Create Rivet client
import { RivetClient } from '@rivet-gg/api';
const RIVET = new RivetClient({ token: '[YOUR TOKEN HERE]' });

// Make request
await RIVET.identity.links.cancel({
  // Add your request body here
});
```

</CodeGroup>

## Request Parameters

## Request Body

### `identity_link_token`

`string` (required)

Documentation at https://jwt.io/

## Response Body

_Empty response body._
