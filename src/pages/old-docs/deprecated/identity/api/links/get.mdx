import { CodeGroup, Code } from '@/components/Code';

# links.get

## Description

Returns the current status of a linking process. Once `status` is `complete`, the identity's profile should be fetched again since they may have switched accounts.

## Code Examples

<CodeGroup title='Request' tag='GET' label='https://api.rivet.gg/identity/game-links'>

```bash {{ "title": "cURL" }}
curl -X GET 'https://api.rivet.gg/identity/game-links'
```

```ts
// Create Rivet client
import { RivetClient } from '@rivet-gg/api';
const RIVET = new RivetClient({ token: '[YOUR TOKEN HERE]' });

// Make request
await RIVET.identity.links.get({
  // Add your request body here
});
```

</CodeGroup>

## Request Parameters

### `identity_link_token`

_Query parameter, required_

### `watch_index`

_Query parameter, optional_

## Response Body

### `current_identity`

`object` (required)

An identity handle.

### `current_identity.account_number`

`integer` (required)

### `current_identity.avatar_url`

`string` (required)

The URL of this identity's avatar image.

### `current_identity.display_name`

`string` (required)

### `current_identity.external`

`object` (required)

External links for an identity.

### `current_identity.external.profile`

`string` (required)

A link to this identity's profile page.

### `current_identity.external.settings`

`string`

A link to the Rivet settings page.

### `current_identity.identity_id`

`string` (required)

### `current_identity.is_registered`

`boolean` (required)

Whether or not this identity is registered with a linked account.

### `current_identity.presence`

`object`

Information about the identity's current status, party, and active game.

### `current_identity.presence.game_activity`

`object`

The game an identity is currently participating in.

### `current_identity.presence.game_activity.game`

`object` (required)

### `current_identity.presence.game_activity.game.banner_url`

`string`

The URL of this game's banner image.

### `current_identity.presence.game_activity.game.display_name`

`string` (required)

### `current_identity.presence.game_activity.game.game_id`

`string` (required)

### `current_identity.presence.game_activity.game.logo_url`

`string`

The URL of this game's logo image.

### `current_identity.presence.game_activity.game.name_id`

`string` (required)

A human readable short identifier used to references resources. Different than a `uuid` because this is intended to be human readable. Different than `DisplayName` because this should not include special characters and be short.

### `current_identity.presence.game_activity.message`

`string` (required)

A short activity message about the current game activity.

### `current_identity.presence.game_activity.mutual_metadata`

`object`

JSON data seen only by the given identity and their mutual followers.

### `current_identity.presence.game_activity.public_metadata`

`object`

JSON data seen by anyone.

### `current_identity.presence.status`

`string` (required)

The current status of an identity. This helps players understand if another player is currently playing or has their game in the background.

### `current_identity.presence.update_ts`

`string` (required)

### `game`

`object` (required)

### `game.banner_url`

`string`

The URL of this game's banner image.

### `game.display_name`

`string` (required)

### `game.game_id`

`string` (required)

### `game.logo_url`

`string`

The URL of this game's logo image.

### `game.name_id`

`string` (required)

A human readable short identifier used to references resources. Different than a `uuid` because this is intended to be human readable. Different than `DisplayName` because this should not include special characters and be short.

### `new_identity`

`object`

### `new_identity.identity`

`object` (required)

An identity profile.

### `new_identity.identity.account_number`

`integer` (required)

### `new_identity.identity.avatar_url`

`string` (required)

The URL of this identity's avatar image.

### `new_identity.identity.awaiting_deletion`

`boolean`

Whether or not this identity is awaiting account deletion. Only visible to when the requestee is
this identity.

### `new_identity.identity.bio`

`string` (required)

Follows regex ^(?:[^\n\r]+\n?|\n){1,5}$

### `new_identity.identity.display_name`

`string` (required)

### `new_identity.identity.external`

`object` (required)

External links for an identity.

### `new_identity.identity.external.profile`

`string` (required)

A link to this identity's profile page.

### `new_identity.identity.external.settings`

`string`

A link to the Rivet settings page.

### `new_identity.identity.follower_count`

`integer` (required)

### `new_identity.identity.following`

`boolean` (required)

Whether or not the requestee's identity is following this identity.

### `new_identity.identity.following_count`

`integer` (required)

### `new_identity.identity.games`

`array<object>` (required)

### `new_identity.identity.games[*].game`

`object` (required)

### `new_identity.identity.games[*].game.banner_url`

`string`

The URL of this game's banner image.

### `new_identity.identity.games[*].game.display_name`

`string` (required)

### `new_identity.identity.games[*].game.game_id`

`string` (required)

### `new_identity.identity.games[*].game.logo_url`

`string`

The URL of this game's logo image.

### `new_identity.identity.games[*].game.name_id`

`string` (required)

A human readable short identifier used to references resources. Different than a `uuid` because this is intended to be human readable. Different than `DisplayName` because this should not include special characters and be short.

### `new_identity.identity.games[*].stats`

`array<object>` (required)

### `new_identity.identity.games[*].stats[*].config`

`object` (required)

A game statistic config.

### `new_identity.identity.games[*].stats[*].config.aggregation`

`string` (required)

A value denoting the aggregation method of a game statistic.

### `new_identity.identity.games[*].stats[*].config.display_name`

`string` (required)

### `new_identity.identity.games[*].stats[*].config.format`

`string` (required)

A value denoting the format method of a game statistic.

### `new_identity.identity.games[*].stats[*].config.icon_id`

`string` (required)

### `new_identity.identity.games[*].stats[*].config.postfix_plural`

`string`

A string appended to the end of a game statistic's value that is not exactly 1. Example: 45 **dollars**.

### `new_identity.identity.games[*].stats[*].config.postfix_singular`

`string`

A string appended to the end of a singular game statistic's value. Example: 1 **dollar**.

### `new_identity.identity.games[*].stats[*].config.prefix_plural`

`string`

A string prepended to the beginning of a game statistic's value that is not exactly 1. Example: **values** 45.

### `new_identity.identity.games[*].stats[*].config.prefix_singular`

`string`

A string appended to the beginning of a singular game statistic's value. Example: **value** 1.

### `new_identity.identity.games[*].stats[*].config.priority`

`integer` (required)

### `new_identity.identity.games[*].stats[*].config.record_id`

`string` (required)

### `new_identity.identity.games[*].stats[*].config.sorting`

`string` (required)

A value denoting the sorting method of a game statistic.

### `new_identity.identity.games[*].stats[*].overall_value`

`number` (required)

A single overall value of the given statistic.

### `new_identity.identity.groups`

`array<object>` (required)

### `new_identity.identity.groups[*].group`

`object` (required)

A group handle.

### `new_identity.identity.groups[*].group.avatar_url`

`string`

The URL of this group's avatar image

### `new_identity.identity.groups[*].group.display_name`

`string` (required)

### `new_identity.identity.groups[*].group.external`

`object` (required)

External links for this group.

### `new_identity.identity.groups[*].group.external.profile`

`string` (required)

A link to this group's profile page.

### `new_identity.identity.groups[*].group.group_id`

`string` (required)

### `new_identity.identity.groups[*].group.is_developer`

`boolean`

Whether or not this group is a developer group.

### `new_identity.identity.identity_id`

`string` (required)

### `new_identity.identity.is_admin`

`boolean` (required)

Whether or not this identity is an admin.

### `new_identity.identity.is_following_me`

`boolean` (required)

Whether or not this identity is both following and is followed by the requestee's identity.

### `new_identity.identity.is_game_linked`

`boolean`

Whether or not this game user has been linked through the Rivet dashboard.

### `new_identity.identity.is_mutual_following`

`boolean` (required)

### `new_identity.identity.is_registered`

`boolean` (required)

Whether or not this identity is registered with a linked account.

### `new_identity.identity.join_ts`

`string` (required)

### `new_identity.identity.linked_accounts`

`array<object>` (required)

### `new_identity.identity.linked_accounts[*].access_token`

`object`

An identity's access token identity.

### `new_identity.identity.linked_accounts[*].access_token.name`

`string` (required)

### `new_identity.identity.linked_accounts[*].email`

`object`

An identity's linked email.

### `new_identity.identity.linked_accounts[*].email.email`

`string` (required)

A valid email address

### `new_identity.identity.presence`

`object`

Information about the identity's current status, party, and active game.

### `new_identity.identity.presence.game_activity`

`object`

The game an identity is currently participating in.

### `new_identity.identity.presence.game_activity.game`

`object` (required)

### `new_identity.identity.presence.game_activity.game.banner_url`

`string`

The URL of this game's banner image.

### `new_identity.identity.presence.game_activity.game.display_name`

`string` (required)

### `new_identity.identity.presence.game_activity.game.game_id`

`string` (required)

### `new_identity.identity.presence.game_activity.game.logo_url`

`string`

The URL of this game's logo image.

### `new_identity.identity.presence.game_activity.game.name_id`

`string` (required)

A human readable short identifier used to references resources. Different than a `uuid` because this is intended to be human readable. Different than `DisplayName` because this should not include special characters and be short.

### `new_identity.identity.presence.game_activity.message`

`string` (required)

A short activity message about the current game activity.

### `new_identity.identity.presence.game_activity.mutual_metadata`

`object`

JSON data seen only by the given identity and their mutual followers.

### `new_identity.identity.presence.game_activity.public_metadata`

`object`

JSON data seen by anyone.

### `new_identity.identity.presence.status`

`string` (required)

The current status of an identity. This helps players understand if another player is currently playing or has their game in the background.

### `new_identity.identity.presence.update_ts`

`string` (required)

### `new_identity.identity_token`

`string` (required)

Documentation at https://jwt.io/

### `new_identity.identity_token_expire_ts`

`string` (required)

### `status`

`string` (required)

### `watch`

`object` (required)

Provided by watchable endpoints used in blocking loops.

### `watch.index`

`string` (required)

Index indicating the version of the data responded.
Pass this to `WatchQuery` to block and wait for the next response.
