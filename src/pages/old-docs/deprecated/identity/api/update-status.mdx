import { CodeGroup, Code } from '@/components/Code';

# updateStatus

## Description

Updates the current identity's status.

## Code Examples

<CodeGroup title='Request' tag='POST' label='https://api.rivet.gg/identity/identities/identities/self/status'>

```bash {{ "title": "cURL" }}
# Write the request body to body.json before running
curl -X POST -d '@body.json' 'https://api.rivet.gg/identity/identities/identities/self/status'
```

```ts
// Create Rivet client
import { RivetClient } from '@rivet-gg/api';
const RIVET = new RivetClient({ token: '[YOUR TOKEN HERE]' });

// Make request
await RIVET.identity.updateStatus({
  // Add your request body here
});
```

</CodeGroup>

## Request Parameters

## Request Body

### `status`

`string` (required)

The current status of an identity. This helps players understand if another player is currently playing or has their game in the background.

## Response Body

_Empty response body._
