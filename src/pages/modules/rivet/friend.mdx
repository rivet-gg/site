import { faBookOpen } from '@fortawesome/pro-solid-svg-icons';

# friend

_Enable users follow eachother to create friendships._

|                 |       |
| --------------- | ----- |
| **Author**      | rivet |
| **Identifier**  | friend  |
| **Version**     | 1.0.0 |

## Installation

To install this module, copy and paste this code into your Rivet configuration at `rivet.toml`. Then, run `rivet deploy -n prod`.

<CodeGroup title="rivet.toml">
```toml
[modules]
    friend = "rivet/friend"

[friend]
    # Configuration options
```
</CodeGroup>

## Usage

<ResourceGroup title={null}>
  <Resource title='Documentation' icon={faBookOpen} iconType='duotone' href='/docs/friend' />
</ResourceGroup>
```
