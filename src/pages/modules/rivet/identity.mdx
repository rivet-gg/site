import { faBookOpen } from '@fortawesome/pro-solid-svg-icons';

# identity

_Enable users to sign in to your application._

|                 |       |
| --------------- | ----- |
| **Author**      | rivet |
| **Identifier**  | identity  |
| **Version**     | 1.0.0 |

## Installation

To install this module, copy and paste this code into your Rivet configuration at `rivet.toml`. Then, run `rivet deploy -n prod`.

<CodeGroup title="rivet.toml">
```toml
[modules]
    identity = "rivet/identity"

[identity]
    # Configuration options
```
</CodeGroup>

## Usage

<ResourceGroup title={null}>
  <Resource title='Documentation' icon={faBookOpen} iconType='duotone' href='/docs/identity' />
</ResourceGroup>
```
