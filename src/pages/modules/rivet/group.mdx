import { faBookOpen } from '@fortawesome/pro-solid-svg-icons';

# group

_Enable users to join groups, manage memberships, and more._

|                 |       |
| --------------- | ----- |
| **Author**      | rivet |
| **Identifier**  | group  |
| **Version**     | 1.0.0 |

## Installation

To install this module, copy and paste this code into your Rivet configuration at `rivet.toml`. Then, run `rivet deploy -n prod`.

<CodeGroup title="rivet.toml">
```toml
[modules]
    group = "rivet/group"

[group]
    # Configuration options
```
</CodeGroup>

## Usage

<ResourceGroup title={null}>
  <Resource title='Documentation' icon={faBookOpen} iconType='duotone' href='/docs/group' />
</ResourceGroup>
```
