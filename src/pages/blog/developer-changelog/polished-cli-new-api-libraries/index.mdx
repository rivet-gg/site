import { ArticleLayout } from '@/components/ArticleLayout';
import image from './image.png';

export const meta = {
  image,
  author: 'nathan-flurry',
  date: '2023-02-13',
  title: `Polished CLI, new API libraries, and more`,
  description: `New JavaScript/TypeScript API library, published OpenAPI spec, published new CLI, cloud version configs are now easier to write with game mode`,
  imageAlt: `refreshed api libraries and new cli deploys`,
};

export default props => <ArticleLayout image={image} meta={meta} {...props} />;

Hell, world!
      